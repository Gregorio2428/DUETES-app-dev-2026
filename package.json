{
  "name": "latch-auth",
  "version": "1.0.0",
  "description": "Simple signup and login system built with Express.js. Every API endpoint is a POST.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2"
  }
}
